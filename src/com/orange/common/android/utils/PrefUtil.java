package com.orange.common.android.utils;


public class PrefUtil {
	/* There should be no Preference Util (this just a "position holder"), since:
	 * 1) preference is already easy to use
	 * 2) the preference name should always app specific
	 * 3) adding multiple preference is not easy to extract, as need commit in same editor
	 */
}
